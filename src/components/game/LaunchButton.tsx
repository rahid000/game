
"use client";

import type { FC } from 'react';

// This component is no longer in active use and its functionality has been moved to src/app/page.tsx.
// It's kept as an empty shell to avoid breaking potential imports if any were missed, though it should be safe to delete.
const LaunchButton: FC = () => {
  return null;
};

export default LaunchButton;
